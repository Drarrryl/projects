import time
from tkinter import *
from PIL import Image, ImageShow, ImageTk
from games import mainMenu
"""
print("What would you like to play?")

games = ["Tic Tac Toe", "Runner"]
gameList = "Choose a game!\n"
for i in range(len(games)):
    gameList += games[i]
    gameList += "\n"
gameChoice = input(gameList)
"""

mainMenu()
